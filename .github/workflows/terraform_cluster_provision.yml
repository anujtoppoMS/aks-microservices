name: Terraform CI/CD

on:
  workflow_dispatch:
#   push:
#     branches:
#       - main
#       - staging
#       - dev

jobs:
  validate:
    name: Terraform Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          cli_config_credentials_token: ${{ secrets.TFC_TOKEN }}

      - name: Terraform Init
        run: terraform init #-backend-config="workspace=${{ github.ref_name }}"
        working-directory: ./aks-microservices/terraform/envs/prod

      - name: Terraform Validate
        run: terraform validate

  plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          cli_config_credentials_token: ${{ secrets.TFC_TOKEN }}

      - name: Terraform Init
        run: terraform init #-backend-config="workspace=${{ github.ref_name }}"
        working-directory: ./aks-microservices/terraform/envs/prod

      - name: Terraform Plan
        run: terraform plan -out=tfplan
        working-directory: ./aks-microservices/terraform/envs/prod

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: ./aks-microservices/terraform/envs/prod/tfplan

  apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: plan
    if: github.ref == 'refs/heads/main'   # only auto-apply on main
    environment:
      name: production
      url: https://app.terraform.io
    steps:
      - uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          cli_config_credentials_token: ${{ secrets.TFC_TOKEN }}

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan

      - name: Move Plan To Working Directory
        run: |
            mv tfplan ./aks-microservices/terraform/envs/prod/

      - name: Terraform Apply
        run: |
          terraform init
          terraform apply -auto-approve tfplan
        working-directory: ./aks-microservices/terraform/envs/prod